# Client (React)

Dev server (Vite):

```powershell
cd project/client
npm install
npm run dev
```

The client communicates with the backend at `/api/*`. The backend must be running (CORS is enabled in the server).
